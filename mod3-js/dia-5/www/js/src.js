'use strict';

// Array
const nums = [3, 13, 54, 2, 32];

for (let i = 0; i < nums.length; i++) {
    const num = nums[i];
    console.log(num);
}

const usernames = ['Esteban', 'Lola', 'Manu'];

for (const name of usernames) {
    console.log(name);
}
